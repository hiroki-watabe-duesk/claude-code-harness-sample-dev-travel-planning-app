# スプリント9 評価レポート

評価日: 2026-06-01
対象機能: 機能15（持ち物・準備リスト）/ 機能16（旅行マップのプレビュー）
検証方法: Playwright（`@playwright/mcp` 同梱の chromium）による実ブラウザ操作。`npm install && npm start` で http://localhost:5180/ を起動（サンプル生成モード＝モック）。検証スクリプト: `.playwright-mcp/sprint9-verify.mjs` / `.playwright-mcp/sprint9-regcheck.mjs`。

## 判定: PASS

## 受け入れ基準の結果

| # | 基準 | 結果 | 証拠 |
|---|------|------|------|
| 1 | 行き先・テーマに応じた持ち物／準備項目のチェックリストが表示される | PASS | 箱根/温泉で `packing-checklist` 表示（12項目）。温泉項目「タオル・手ぬぐい」「着替え・湯上がり用の服」等を含む。テーマを「自然」に変えて再生成すると「歩きやすい靴・スニーカー」「虫除けスプレー」「レインウェア」「飲み物・行動食」に置き換わり、温泉固有4項目が消え自然固有4項目が出現（onsenOnly=4 / natureOnly=4）。スクショ: `.playwright-mcp/sprint9-01-onsen.png`, `sprint9-03-nature.png` |
| 2 | 各項目にチェックを付けることができ、チェック状態が視覚的に分かる | PASS | 先頭項目をクリック → `checked=true`、行 class が `packing__item` → `packing__item--checked` に変化、独自ボックスに「✓」表示、進捗が「0 / 12」→「1 / 12 準備OK」に増加。さらに保存→リロード→再オープンで進捗「1 / 12」が復元（localStorage 保持）。スクショ: `.playwright-mcp/sprint9-02-onsen-checked.png` |
| 3 | プランに含まれるスポットの位置関係を示すマップ／地図的プレビューが表示される | PASS | 結果画面に `map-preview` / `map-svg` が表示。方眼グリッド背景の自前SVGに番号付きピンとラベル、同日スポットを結ぶ破線ルート線を描画。スクショ: `.playwright-mcp/sprint9-04-kyoto-3days-map.png` |
| 4 | マッププレビューに、プラン内の複数スポットが地点・ラベルとして示されている | PASS | 箱根/1泊2日: `map-pin`=8 / `map-label`=8、ラベル8件すべてがタイムラインのスポット名と一致（labelMatchCount=8/8）。京都/2泊3日: `map-pin`=12 / `map-label`=12 / `map-route-*`=3（日数分）。ラベルに実スポット名が表示。 |

**通過率: 4/4 (100%)**

## 発見したバグ
### P0
- なし

### P1
- なし

### P2
- なし
- （参考・バグではない）持ち物リストのチェック状態スコープは「行き先＋テーマ」単位。自己評価の既知問題どおり、同一行き先＋テーマの複数保存プラン間でチェックを共有する。spec はチェック保持を必須としておらず実害は小さい。報告事項に留める。

## リグレッション
- 検出なし。
  - 再生成（スプリント4）: 再生成後も結果・マップ・チェックリストが正しく再描画。
  - スポット削除（スプリント5）: `🗑 削除` で削除すると `map-pin` が 12→11 に減り、削除したスポット名がラベル一覧からも消える（編集がマップへ反映）。`.playwright-mcp/sprint9-05-after-delete-map.png`。
  - 保存・複数管理（スプリント6/7）: プラン保存→保存済み一覧から「開く」で復元、チェック状態も復元。
  - 予算の目安（スプリント8）: `budget-estimate` がマップ／チェックリストと同時に正しく表示。
- コンソールエラー: なし。ページエラー: なし。ネットワークエラー（>=400 / failed）: なし。

## 推奨改善事項（ブロッキングではない）
1. 持ち物チェックの保持スコープを「行き先＋テーマ」から保存プラン単位（保存プランID）に変更すると、同条件の複数プラン間でチェックが混ざらず直感的（spec 必須ではない）。
2. 旅行月（季節）の入力UIは未実装。`buildPackingList` は `plan.month` を受ければ季節セクションを出す実装済みのため、入力UIを足せば季節別項目が活用できる（spec 必須基準は行き先・テーマ起点のため任意）。
3. マップのSVGテキストラベルは長いスポット名で隣接ラベルと重なる場面がある。視認性向上の余地あり（凡例リストで補完されており実害なし）。
