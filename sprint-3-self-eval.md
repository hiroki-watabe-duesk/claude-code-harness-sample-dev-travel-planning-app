# スプリント3 自己評価

対象: スプリント3 — 日程タイムラインと結果の読みやすい表示（機能4: 日程別タイムライン / 機能17: おすすめタグ表示）
実装日: 2026-06-01
実行モード: サンプル生成モード（`ANTHROPIC_API_KEY` 未設定 / `mode: mock`）
起動コマンド: `npm install && npm start`
アプリURL: http://localhost:5180/

## 実装の要点
- `server/planGenerator.js` を構造化データ出力へ拡張。
  - 戻り値に `structuredDays`（`[{ day, title, spots: [{ time, order, name, description, tags }] }]`）を追加。
  - `days` は構造化データの日数（= 泊数 + 1）に一致。`planText`（◆ N日目形式）も構造化データから組み立てて併存（後方互換）。
  - タグはテーマ由来の基底タグ + 人数・日の位置・行き先シードに応じたアクセントタグで動的決定（固定の単一タグにしない / 通常2〜4個）。
  - 実APIは構造化 JSON を依頼し `parseStructuredFromText()` でパース。失敗時はモックの構造化データで補完（UI が常にタイムラインを描ける）。
- フロント:
  - `src/components/DayTimeline.jsx` を新規追加。時刻・スポット名・説明・タグを縦のタイムライン（点と縦線）で表示。
  - `src/components/PlanResult.jsx` を構造化タイムライン表示へ刷新。複数日のときは日タブ（`.daytabs`）を表示し、タブで該当日へスムーズスクロール + アクティブ強調。全日程は同一画面に縦に並ぶためスクロールでも全閲覧可能。
  - 構造化データが無い場合（API フォールバック等）は従来のテキスト表示にフォールバック。
  - `src/styles/global.css` に日タブ・タイムライン・スポットタグのスタイルを追加。

## 受け入れ基準の結果
| 基準 | ステータス | 備考 |
|------|-----------|------|
| 生成結果が日別（1日目・2日目…）のセクションに分かれて表示される | PASS | `structuredDays` の各日が `DayTimeline`（`.dayblock`、見出し「N日目」）として独立セクションでレンダリングされる。2泊3日で 1日目/2日目/3日目 の3セクションを確認。 |
| 各日のセクション内に、時間帯または順序とスポット名・説明が並んだタイムライン状の表示がある | PASS | 各日に `09:00 / 12:00 / 14:00 / 18:00（最終日は16:30）` の時刻付きでスポット名・説明が並ぶ `<ol class="timeline">`。点と縦線でタイムライン状に表示。 |
| 各スポットに特徴を示すタグ（例:「絶景」「グルメ」など）が1つ以上表示される | PASS | 全スポットに `.spottag` が複数表示（例: 自然テーマ「絶景・朝がおすすめ・写真映え」、温泉テーマ「温泉・朝がおすすめ・穴場」）。固定の単一タグではなくテーマ・人数・行き先で動的変化。 |
| 複数日のプランで、日のセクションを切り替えまたはスクロールして全日程を閲覧できる | PASS | 複数日のとき `.daytabs` の日タブを表示。タブクリックで該当日へ `scrollIntoView` + アクティブ強調。全日程が縦に並ぶためスクロールでも全閲覧可能（切り替え・スクロール両対応）。 |

## 既知の問題
- なし（ブロッキングなし）。
- 補足: 日タブはセクションへスクロールする補助で、各日セクション自体は常に描画される（特定日のみ表示する完全フィルタではない）。受け入れ基準「切り替えまたはスクロール」を満たす設計。
- 補足: ヘッドレス Chromium での一部日本語グリフ□（豆腐）表示はレンダリング環境側の事象で、DOM テキストは正しい（スプリント2フィードバックと同様）。アプリの不具合ではない。

## リグレッション確認（スプリント1・2基準の維持）
- 行き先連動: スポット名・説明に行き先名が反映（札幌→「札幌・…」、内容も行き先ごとに異なる）。
- 泊数で日数分: nights=2 → 3セクション（4日目なし）、nights=0（日帰り）→ 1セクションを確認。
- テーマで内容変化: standard と onsen で `planText`・spots ともに差分あり。
- 人数反映: people>=3 で「みんなで」、people=1 で「ひとり旅向き」タグ、説明文に人数表現を反映。
- ローディング→結果切り替え、再読み込みで初期化、行き先未入力エラーは `App.jsx` の既存フロー維持（コード変更なし）。

## 確認手順
1. `npm install && npm start` を実行。
2. ブラウザで http://localhost:5180/ を開く。
3. 行き先「京都」、泊数「2泊3日」、人数「3」、テーマ「自然」を選び「プランを作成」。
4. 結果に日タブ（1日目/2日目/3日目）と各日のタイムライン（時刻・スポット名・タグ・説明）が表示されることを確認。
5. 日タブをクリックすると該当日へスクロールし、タブがアクティブ表示になることを確認。
6. テーマを「温泉」に変えて再生成し、スポット名・タグ・説明が変わることを確認。
7. API 直接確認: `curl -s -X POST http://localhost:5180/api/plan -H 'content-type: application/json' -d '{"destination":"金沢","nights":2,"people":4,"theme":"history"}'` で `structuredDays` が3日分・各日4スポット・各スポットに `tags` 配列を含むことを確認。

## 信頼スコア
総合: 9/10

## エバリュエーターへの引き渡し可否
YES — スプリント3の全受け入れ基準を満たし、スプリント1・2のリグレッションも確認済み。
