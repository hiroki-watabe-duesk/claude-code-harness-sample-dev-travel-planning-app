# スプリント10 自己評価

対象: 機能18（オンボーディング／使い方ガイド）＋ 全体のデザイン仕上げ
実装日: 2026-06-01
技術スタック: PROGRESS.md 確定どおり（React 18 + Vite 5 / Express 4 / Node.js 22、永続化はブラウザ内 localStorage）。技術切り替えなし。

## 実装サマリ
- `src/hooks/useOnboarding.js` — localStorage の `tabinavi.onboardingSeen.v1` に「見終えた」フラグ（boolean）だけを保持。初回訪問（未読）時のみ自動表示。`dismiss()` で `true` を記録し2回目以降は自動表示しない。`reopen()` で再表示。
- `src/components/Onboarding.jsx` — 使い方3ステップ（行き先入力→条件選択→プラン作成）＋入力例3件（京都/歴史、札幌/グルメ、箱根/温泉）を提示するモーダル。入力例クリックでフォームへ条件を反映して閉じる（自動生成はしない）。✕／「はじめる」／オーバーレイ外クリック／Esc で閉じられる。
- `src/App.jsx` — `useOnboarding` を組み込み。ヘッダーに「❓使い方」ボタン（`data-testid="open-onboarding"`）を常設し、いつでも案内を再表示できる。`handleTrySample` で入力例をフォームへ反映。
- `src/lib/storage.js` — `ONBOARDING_KEY` を追加。
- `src/styles/global.css` — オンボーディングのモーダル/オーバーレイ、ヘッダーの「使い方」ボタンを追加。既存のデザイントークン（teal+orange の配色、余白、角丸、影、Zen Maru Gothic/Noto Sans JP フォント、日本語フォールバック）を再利用し全画面で一貫。レスポンシブ調整。

## 受け入れ基準の結果
| 基準 | ステータス | 備考 |
|------|-----------|------|
| 初回訪問時に、使い方・入力例を示す案内が表示される | PASS | 初回 `goto` で `[data-testid="onboarding"]` が自動表示。3ステップ＋入力例3件（`onboarding-sample`）を表示。Playwright で確認。スクショ `sprint10-01-onboarding.png` |
| 案内は閉じることができ、閉じた後に通常のトップ画面が利用できる | PASS | 「はじめる」「✕」「オーバーレイ外クリック」「Esc」で閉じる。閉じた後 `#destination` が有効でフォーム操作可能。スクショ `sprint10-02-top-after-close.png` |
| アプリ全体で配色・余白・見出しのデザインに一貫性がある | PASS | 既存の `:root` デザイントークンを全画面（トップ／生成中＝ローディング／結果／お気に入り・保存・複数管理／予算／持ち物／マップ／オンボーディング）で統一使用。フルページのデスクトップ/モバイルスクショで視覚的整合を確認 |
| 主要操作（入力・生成・閲覧・保存）を行ってもレイアウト崩れ・操作不能が発生しない | PASS | 入力→生成→日別タイムライン閲覧（3 dayblock）→予算表示→保存（通知表示）まで一通り実行。デスクトップ・モバイル幅とも横 overflow=0px。console/page/network エラー 0 件 |

**自己評価 通過: 4/4**

## 自前検証（Playwright, headless chromium）
- スクリプト: `.playwright-mcp/sprint10-verify.mjs`（`npm start` 起動の http://localhost:5180/ に対して実行）
- 結果: **13/13 checks passed**、consoleErrors=0 / pageErrors=0 / netErrors=0
- 主な確認項目:
  1. 初回訪問でオンボーディング自動表示（入力例 samples=3）
  2. 「はじめる」で閉じる → フォーム利用可能
  3. リロード後は自動表示されない、かつトップは初期状態（`#destination` が空）= スプリント1基準を非破壊
  4. 「使い方」ボタンで再表示できる／入力例クリックで閉じてフォームへ反映（dest="京都"）
  5. 生成→3日分の dayblock 表示／予算表示／保存通知／横 overflow=0px（デスクトップ・モバイル）

## オンボーディング表示制御の確認手順（手動再現）
1. ブラウザの localStorage から `tabinavi.onboardingSeen.v1` を削除（または初回アクセス相当の新規プロファイル）。
2. http://localhost:5180/ を開く → **オンボーディングが自動表示される**。
3. 「はじめる」または✕で閉じる → トップ画面が普通に使える。
4. ページをリロード → **オンボーディングは自動表示されない**。かつ入力欄は空（初期状態）。
5. ヘッダーの「❓使い方」を押す → **再びオンボーディングが表示される**（任意の再閲覧導線）。

## 既知の問題 / 判断メモ
- 入力例クリックは「フォームへ条件を反映して案内を閉じる」挙動とし、ワンクリックでの自動生成はしない。spec は「入力例を示す案内」を求めており、ユーザーが内容を確認してから生成できる方が安全と判断（曖昧点の最もシンプルな解釈）。
- オンボーディングの永続化対象は「見終えたフラグ」のみ。プラン・入力状態は永続化しないため、スプリント1の「リロードで初期状態のトップに戻る」を一切壊さない。
- モーダルは縦に長い場合オーバーレイ/ダイアログ自体がスクロール可能（`overflow-y: auto`、`max-height: calc(100vh - 40px)`）。小さい高さのビューポートでも「はじめる」へ到達可能（Playwright でクリック成功を確認）。
- 旅行月（季節）入力UIはスプリント9同様に未導入（spec 必須外）。デザイン仕上げの範囲では追加していない。

## 信頼スコア
総合: 9/10
（全受け入れ基準 PASS・自前検証 13/13・エラー0。満点としない理由はエバリュエーターの実ブラウザ検証を経ていないため）

## エバリュエーターへの引き渡し可否
YES — `npm start` で http://localhost:5180/ にて全機能が動作。スプリント10の4基準を満たし、スプリント1〜9のリグレッションも自前検証で未検出。
