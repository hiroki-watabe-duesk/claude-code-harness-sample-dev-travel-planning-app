# 製品仕様書: たびナビ（AI旅行プランナー）

## ビジョン
「たびナビ」は、行きたい日本国内の都道府県や都市を指定するだけで、AIが旅行に最適な日程・観光スポット・モデルコースを提案してくれるWebアプリケーションである。対象ユーザーは、旅行の計画に時間をかけたくない、あるいは何から決めればよいか分からない国内旅行の検討者。核心的な価値は「行き先を入力するだけで、ゼロから自分で調べることなく、すぐに使える具体的な旅行プランが手に入る」体験を提供することにある。本サンプルではデータベースと認証を持たず、利用者の操作はブラウザ内で完結する。プラン生成にはClaude APIを用い、AIが文脈に応じた旅行プランを動的に生成する。

## 機能一覧

- **機能1: 行き先の指定** — ユーザーが旅行したい都道府県・都市をフォームに入力して指定できる。
- **機能2: 旅行条件の入力** — ユーザーが旅行日数（泊数）、旅行人数、旅行のテーマ（グルメ・自然・歴史・温泉など）を選択・入力できる。
- **機能3: AIによる旅行プラン生成** — 入力した行き先と条件をもとに、AIが日別のモデルコースを含む旅行プランを生成して表示する。
- **機能4: 日程別タイムラインの表示** — 生成されたプランを「1日目・2日目…」の時系列タイムライン形式で、時間帯・スポット名・説明とともに閲覧できる。
- **機能5: 生成中のローディング表示** — プラン生成中であることがアニメーションやメッセージで分かり、完了すると結果に切り替わる。
- **機能6: エラー時のフィードバック** — 行き先未入力やプラン生成失敗時に、原因と次の操作が分かるメッセージが表示される。
- **機能7: プランの再生成** — 同じ条件、または条件を変更してプランを作り直すことができる。
- **機能8: スポットの詳細表示** — タイムライン上の各スポットをクリック・展開すると、説明・おすすめポイント・滞在目安などの詳細が見られる。
- **機能9: プランの編集（スポットの削除・並べ替え）** — ユーザーが提案されたスポットを削除したり、日程内・日程間で順序を入れ替えたりできる。
- **機能10: お気に入りスポットの保存** — 気に入ったスポットにマークを付け、お気に入り一覧として確認できる（ブラウザ内に保持）。
- **機能11: プランの保存と復元** — 生成・編集したプランをブラウザ内に保存し、再訪問時に読み込んで続きから閲覧できる。
- **機能12: 複数プランの管理** — 保存した複数のプランを一覧表示し、開く・名前変更・削除ができる。
- **機能13: プランのエクスポート** — 完成したプランをテキスト等の持ち出しやすい形式でコピーまたはダウンロードできる。
- **機能14: 予算の目安表示** — プランに対して概算の費用イメージ（移動・宿泊・食事・観光の目安）を確認できる。
- **機能15: 持ち物・準備リストの提案** — 行き先・季節・テーマに応じた持ち物や事前準備のチェックリストが提示され、チェックを付けられる。
- **機能16: 旅行マップのプレビュー** — プランに含まれるスポットの位置関係を視覚的に把握できるマップ／地図的なプレビューを表示する。
- **機能17: テーマ・季節に応じたおすすめタグ表示** — 各スポットに「絶景」「子連れ向き」「雨の日OK」などのタグが付与され、特徴が一目で分かる。
- **機能18: オンボーディング／使い方ガイド** — 初回利用者に向けて、アプリの使い方と入力例を示す簡単な案内が表示される。

## スプリント計画

### スプリント1: コアループ（行き先入力からAIプラン生成まで）
**ゴール:** ユーザーが行き先を入力してボタンを押すと、AIが生成した旅行プランが画面に表示される、という最小の中核体験が動作している。
**対象機能:** 機能1, 機能3, 機能5
**受け入れ基準:**
- トップ画面に行き先を入力するテキスト入力欄と「プランを作成」操作ボタンが表示されている。
- 行き先（例:「京都」）を入力してボタンを押すと、生成中であることを示すローディング表示が現れる。
- 生成完了後、入力した行き先に関連する旅行プランの文章・内容が画面に表示される（プレースホルダの固定文ではなく、入力に応じた内容であること）。
- ページを再読み込みすると初期状態（入力欄が空のトップ画面）に戻る。

### スプリント2: 旅行条件の指定とプラン精度の向上
**ゴール:** ユーザーが日数・人数・テーマを指定でき、それらの条件が反映された旅行プランが生成される。
**対象機能:** 機能2
**受け入れ基準:**
- 行き先に加えて、泊数（日数）を選択または入力できるコントロールが表示されている。
- 人数を指定できるコントロールが表示されている。
- 旅行テーマ（例: グルメ・自然・歴史・温泉など複数の選択肢）を選択できるコントロールが表示されている。
- 例として泊数を「2泊3日」に設定して生成すると、生成結果に複数日分（1日目・2日目・3日目）の内容が含まれる。
- テーマを「温泉」に変更して再生成すると、結果の内容が変更前と異なるものになる。

### スプリント3: 日程タイムラインと結果の読みやすい表示
**ゴール:** 生成されたプランが「1日目・2日目…」のタイムライン形式で、時間帯ごとに整理されて読みやすく表示される。
**対象機能:** 機能4, 機能17
**受け入れ基準:**
- 生成結果が日別（1日目・2日目…）のセクションに分かれて表示される。
- 各日のセクション内に、時間帯または順序とスポット名・説明が並んだタイムライン状の表示がある。
- 各スポットに特徴を示すタグ（例:「絶景」「グルメ」など）が1つ以上表示される。
- 複数日のプランで、日のセクションを切り替えまたはスクロールして全日程を閲覧できる。

### スプリント4: スポット詳細とプランの再生成・エラー処理
**ゴール:** 各スポットの詳細を確認でき、条件を変えての再生成や、入力不備・生成失敗時の分かりやすいフィードバックが機能している。
**対象機能:** 機能6, 機能7, 機能8
**受け入れ基準:**
- タイムライン上のスポットをクリックまたは展開すると、説明・おすすめポイント・滞在目安などの詳細情報が表示される。
- 行き先を入力せずに生成しようとすると、入力を促すエラーメッセージが表示され、生成が始まらない。
- 結果表示後に「再生成」操作を行うと、新しいプランの生成が始まり、結果が更新される。
- 条件（例: テーマ）を変更して再生成すると、変更が反映された新しい結果に切り替わる。

### スプリント5: プランの編集（削除・並べ替え）
**ゴール:** ユーザーが提案されたプランを自分好みに編集（スポットの削除・順序変更）でき、編集結果が画面に即座に反映される。
**対象機能:** 機能9
**受け入れ基準:**
- 各スポットに削除操作があり、削除するとそのスポットがタイムラインから消える。
- スポットの順序を変更する操作（上下移動またはドラッグ等）があり、操作後にタイムライン上の並び順が変わる。
- 編集後の状態が、別の操作（スクロールやタブ切り替え等）を行っても保持されている。
- 削除・並べ替えの操作後、ページ内の日程の見出しや件数表示が編集結果と整合している。

### スプリント6: お気に入りとプランの保存・復元
**ゴール:** ユーザーが気に入ったスポットをお気に入り登録でき、作成したプランをブラウザに保存して再訪時に復元できる。
**対象機能:** 機能10, 機能11
**受け入れ基準:**
- 各スポットにお気に入り登録の操作があり、登録するとお気に入り状態が視覚的に分かる（アイコンの変化等）。
- お気に入りに登録したスポットの一覧を確認できる画面・領域がある。
- 現在のプランを保存する操作があり、保存後にページを再読み込みしても保存したプランを読み込んで再表示できる。
- 復元したプランが、保存時点の編集内容（並び順・削除状態）を反映している。

### スプリント7: 複数プランの管理
**ゴール:** ユーザーが複数の保存済みプランを一覧で管理（開く・名前変更・削除）できる。
**対象機能:** 機能12
**受け入れ基準:**
- 保存済みプランの一覧が表示され、各プランの名前または行き先が確認できる。
- 異なる条件で作成した2つ以上のプランをそれぞれ保存でき、一覧に複数件として表示される。
- 一覧からプランを選んで開くと、その内容が画面に表示される。
- プランの名前を変更でき、変更後の名前が一覧に反映される。
- プランを削除でき、削除後は一覧から消える。

### スプリント8: エクスポートと予算の目安
**ゴール:** 完成したプランを外部に持ち出せる形で出力でき、旅行費用の概算イメージを確認できる。
**対象機能:** 機能13, 機能14
**受け入れ基準:**
- プランをコピーまたはダウンロードできる操作があり、実行すると行き先・日程・スポットを含む内容が出力される（コピー成功の通知、またはファイルの取得が確認できる）。
- 出力された内容に、画面上のプランの主要情報（行き先・各日のスポット名）が含まれている。
- プランに対して概算費用の目安（移動・宿泊・食事・観光などの内訳または合計）が表示される。
- 条件（泊数や人数）を変えて生成すると、表示される費用の目安もそれに応じて変化する。

### スプリント9: 持ち物リストと旅行マッププレビュー
**ゴール:** 行き先・テーマに応じた持ち物チェックリストと、スポットの位置関係を示すマッププレビューが利用できる。
**対象機能:** 機能15, 機能16
**受け入れ基準:**
- 行き先・テーマに応じた持ち物／準備項目のチェックリストが表示される。
- 各項目にチェックを付けることができ、チェック状態が視覚的に分かる。
- プランに含まれるスポットの位置関係を示すマップまたは地図的なプレビューが表示される。
- マッププレビューに、プラン内の複数スポットが地点・ラベルとして示されている。

### スプリント10: オンボーディングと仕上げ
**ゴール:** 初回利用者が使い方を理解でき、アプリ全体が旅行プランナーにふさわしい魅力的で一貫したデザインに仕上がっている。
**対象機能:** 機能18（および全体のデザイン仕上げ）
**受け入れ基準:**
- 初回訪問時に、アプリの使い方や入力例を示す案内（ガイド・ヒント・サンプル入力など）が表示される。
- 案内は閉じることができ、閉じた後に通常のトップ画面が利用できる。
- アプリ全体で配色・余白・見出しなどのデザインに一貫性があり、トップ・生成中・結果・管理の各画面が視覚的に整っている。
- 主要な操作（入力・生成・閲覧・保存）を一通り行っても、レイアウト崩れや操作不能な状態が発生しない。

## 用語集
- **プラン（旅行プラン）** — 行き先と条件をもとに生成される、日別のモデルコースを含む旅行の計画一式。
- **スポット** — 観光地・飲食店・宿泊施設など、プランに含まれる個々の訪問先。
- **タイムライン** — 1日の中でスポットを時間帯・順序に沿って並べた時系列の表示形式。
- **テーマ** — 旅行の方向性を表す分類（グルメ・自然・歴史・温泉など）。プラン生成の傾向に影響する。
- **泊数（日数）** — 旅行の期間。「2泊3日」のように、生成されるプランの日数を決める。
- **お気に入り** — ユーザーが気に入ったスポットに付けるマークと、その集合。
- **再生成** — 同じまたは変更した条件で、プランを作り直す操作。
- **エクスポート** — 完成したプランをアプリ外で利用できる形式で出力すること。
- **予算の目安** — 移動・宿泊・食事・観光などにかかる概算費用のイメージ。
- **オンボーディング** — 初回利用者にアプリの使い方を案内する導入体験。
