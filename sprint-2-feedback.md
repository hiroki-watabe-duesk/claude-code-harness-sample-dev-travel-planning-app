# スプリント2 評価レポート

評価日: 2026-06-01
評価者: エバリュエーター（Playwright 実機操作）
対象: スプリント2 — 旅行条件の指定（機能2: 泊数・人数・テーマ）
実行モード: サンプル生成モード（`ANTHROPIC_API_KEY` 未設定 / `mode: mock`）
起動コマンド: `npm install && npm start`
アプリURL: http://localhost:5180/

## 判定: PASS

## 受け入れ基準の結果
| # | 基準 | 結果 | 証拠 |
|---|------|------|------|
| 1 | 行き先に加えて、泊数（日数）を選択／入力できるコントロールが表示されている | PASS | 初期画面に「泊数（日数）」select（`#nights`）が表示。選択肢=「日帰り/1泊2日/2泊3日/3泊4日/4泊5日/5泊6日」を実測。スクリーンショット: `sprint2-01-initial-form.png` |
| 2 | 人数を指定できるコントロールが表示されている | PASS | 「人数」ステッパー（`#people`、min=1 max=20、−/＋ボタン）を表示。＋で 2→3、−で 3→2 に変化することを実機クリックで確認。スクリーンショット: `sprint2-01-initial-form.png` |
| 3 | 旅行テーマ（複数の選択肢）を選択できるコントロールが表示されている | PASS | 「旅行テーマ」select（`#theme`）を表示。選択肢=「おまかせ（王道）/グルメ/自然/歴史/温泉/子連れ・ファミリー」の6種を実測。`GET /api/options` も同6テーマを返却し整合。 |
| 4 | 泊数を「2泊3日」に設定して生成すると複数日分（1日目・2日目・3日目）の内容が含まれる | PASS | 京都・2泊3日（nights=2）・人数2・おまかせで生成。結果本文に「1日目」「2日目」「3日目」が含まれ、「4日目」は含まれないことを実測（`◆ 1日目/◆ 2日目/◆ 3日目` の3見出しのみ）。条件タグ「🗓 2泊3日」表示。スクリーンショット: `sprint2-02-kyoto-2n3d-standard.png`。補足: 日帰り(nights=0)では1日目のみ・2日目なしも確認。 |
| 5 | テーマを「温泉」に変更して再生成すると結果の内容が変更前と異なる | PASS | 行き先・泊数・人数を固定したまま theme を「温泉」に変更して再生成。本文がおまかせ版と明確に異なり（diff で差分あり、文字数 2507→2573）、温泉版は「朝風呂/名湯/湯けむり/外湯・足湯」等の温泉語彙を含む。固定文ではない証拠。条件タグ「🎯 温泉」表示。スクリーンショット: `sprint2-03-kyoto-2n3d-onsen.png` |

**通過率: 5/5 (100%)**

## 発見したバグ
### P0
- なし

### P1
- なし

### P2
- なし（後述の「外観上の注記」はアプリ側のバグではない）

## 外観上の注記（非ブロッキング・アプリのバグではない）
- 取得したスクリーンショット内で、ボタン文言「プランを作成」やステッパー単位「名」などの一部文字が□（豆腐）で表示される箇所がある。これはヘッドレス Chromium に該当日本語/特殊グリフのフォントが揃っていないレンダリング環境側の事象であり、DOM のテキスト内容は正しい（Playwright のテキスト断定で「プランを作成」「名」を確認済み）。実ユーザー環境（通常ブラウザ）では正常表示される。アプリの不具合ではないため修正不要。

## リグレッション
検出なし。スプリント1の主要フローを再確認:
- 生成時にローディング状態（`status==='loading'`）が発生し、完了後に結果へ切り替わる（`LoadingState.jsx` 健在、AC4 検証時に loadingSeen=true）。
- ページ再読み込みで初期状態に復帰（`#destination` 空・`.result` 0件）を実測。
- 行き先未入力で生成すると `role="alert"` の「行き先を入力してください。」が表示され、結果は生成されない（`.result` 0件）。クラッシュなし。
- 行き先に応じて内容が変化する動的生成も維持（京都/札幌/沖縄で内容が異なる）。

## エラーチェック
- コンソールエラー: 0件
- ページ（JS）エラー: 0件
- 失敗したネットワークリクエスト（4xx/5xx/requestfailed）: 0件
- `GET /api/health` → `{"ok":true,"mode":"mock"}`、`GET /api/options` → themes 6件、いずれも 200。

## 推奨改善事項（PASS判定のため任意・非ブロッキング）
1. 生成完了がモックでは一瞬のため、ローディング表示が視認しにくい。最小表示時間（例: 400ms 程度）を設けると、生成中である体験がよりはっきり伝わる（スプリント5の「生成中ローディング」体験向上の余地）。
2. 現状の結果は「◆ N日目」見出し付きテキスト本文。日別タイムラインのカードUI化はスプリント3のスコープとして予定どおりで問題ないが、本文中の日付節の視認性（区切り線等）をわずかに強めると読みやすさが向上する。

## 総合判定
**PASS** — 受け入れ基準 5/5（100%）、P0/P1 バグなし、リグレッションなし、コンソール/ネットワークエラーなし。スプリント3へ進む準備が整っている。
