# スプリント8 評価レポート（エクスポートと予算の目安）

## 判定: PASS

検証方法: 実ブラウザ操作（Playwright / chromium headless）。スクリプト `/.playwright-mcp/sprint8-verify.mjs`。
アプリ起動: `npm install && npm start` → http://localhost:5180/（サンプル生成モード / ANTHROPIC_API_KEY 未設定）。
クリップボード権限を付与（`clipboard-read` / `clipboard-write`）し、ダウンロードイベントを捕捉して中身を実ファイルで検証。

## 受け入れ基準の結果

| # | 基準 | 結果 | 証拠 |
|---|------|------|------|
| 1 | プランをコピーまたはダウンロードできる操作があり、実行すると行き先・日程・スポットを含む内容が出力される（コピー成功通知 or ファイル取得） | PASS | 「📋 コピー」(`copy-plan`) 押下で `export-toast` に「✅ プラン内容をクリップボードにコピーしました。」表示。`navigator.clipboard.readText()` で 868 文字を取得。「⬇️ ダウンロード」(`download-plan`) でダウンロードイベント捕捉、`tabinavi_京都.txt`（868 bytes）を実取得。両出力とも行き先・日程・スポットを含む。 |
| 2 | 出力された内容に画面上のプランの主要情報（行き先・各日のスポット名）が含まれている | PASS | 出力テキストに「行き先: 京都」「■ 1日目」「■ 2日目」を確認。画面の `.timeline__name` から取得した 8 スポット名すべて（8/8）が出力テキストに含まれることを文字列照合で確認。 |
| 3 | 概算費用の目安（移動・宿泊・食事・観光などの内訳または合計）が表示される | PASS | `budget-estimate` パネルに移動 ¥16,000 / 宿泊 ¥18,000 / 食事 ¥16,000 / 観光 ¥9,600 の 4 内訳と合計（`budget-total`）¥59,600・1名あたり ¥29,800 を表示。スクリーンショット `sprint8-01-planA.png` で視覚確認。 |
| 4 | 条件（泊数や人数）を変えて生成すると、表示される費用の目安もそれに応じて変化する | PASS | 京都 1泊2日/2名 → 合計 ¥59,600。京都 2泊3日/4名 → 合計 ¥180,800（移動 ¥32,000・宿泊 ¥72,000・食事 ¥48,000・観光 ¥28,800）と全内訳が増加。さらに 沖縄 日帰り/1名 → 宿泊 ¥0・合計 ¥14,400 と泊数ゼロが宿泊費に反映。2 パターン以上で変化を確認。 |

**通過率: 4/4 (100%)**

## 発見したバグ
### P0
- なし

### P1
- なし

### P2
- なし

## コンソール／ネットワーク
- console error: なし
- pageerror: なし
- ネットワークエラー（HTTP >=400 / failed）: なし

## リグレッション
- 再生成（スプリント4）: 「🔄 このプランを再生成」後も結果・予算パネルが正常表示。検出なし。
- 編集（スプリント5）: スポット削除で観光費 ¥2,400→¥1,800、合計 ¥14,400→¥13,800 と編集が予算に連動。タイムライン整合。検出なし。
- 保存・一覧（スプリント7）: 「💾 このプランを保存」で保存済み一覧に 1 件追加を確認。検出なし。
- 出力が現在表示中プランを反映: 沖縄プランのダウンロードファイルに「沖縄」が含まれることを確認（プラン切替が出力に追従）。

## 推奨改善事項（ブロッキングではない）
1. クリップボード API が使えない／権限拒否の環境向けに `execCommand('copy')` フォールバックは実装済み。実機ブラウザでの権限拒否時の体験は本評価環境では再現していないため対象外（コードレビュー上は妥当）。
2. 予算はテーマで金額が変化しない設計（仕様は泊数・人数の変化を要求しており要件は満たす）。将来テーマ別単価を入れると目安の説得力が増す可能性がある。

## 総合判定
スプリント8 は全 4 基準を実ブラウザ操作で PASS。P0/P1/P2 バグなし、コンソール／ネットワークエラーなし、前スプリント機能のリグレッションなし。
