# スプリント7 自己評価

対象機能: 機能12（複数プランの管理 — 一覧表示・開く・名前変更・削除）

## 起動・確認手順

1. 依存導入とビルド・起動: `npm install && npm start`
2. ブラウザで http://localhost:5180/ を開く（`npm start` の単一プロセス配信）。

### 一連の受け入れ確認手順（2件以上を保存→一覧→開く→名前変更→削除）

1. **1件目を作成・保存**: 行き先「京都」、泊数「1泊」、人数「2名」、テーマ「歴史」で「プランを作成」。生成後に「💾 このプランを保存」を押す → 名前入力ダイアログ（既定名「京都（1泊）」）でOK。
2. フォームの条件を変更し **2件目を作成・保存**: 行き先「金沢」、泊数「2泊」、テーマ「グルメ」で「プランを作成」→「💾 このプランを保存」→ 既定名「金沢（2泊）」でOK。
3. **一覧表示の確認**: 「📁 保存済みプラン」パネルに **2件** 表示され、各行に名前・行き先（📍）・泊数・人数・テーマ・スポット数が出る（件数バッジ「2件」）。
4. **開く**: 「金沢（2泊）」行の「開く」を押すと、画面下に金沢のプランがタイムライン表示され、その行に「表示中」バッジが付く。条件フォームも金沢の条件に同期する。
5. **名前変更**: 「金沢（2泊）」行の「名前変更」を押すとインライン入力に変わる。「金沢グルメ旅」と入力しEnter（または「保存」）。一覧の表示名が「金沢グルメ旅」に更新される。
6. **削除**: 「京都（1泊）」行の「削除」を押すと、その行が一覧から消え件数が「1件」になる。
7. **リロード保持**: ページを再読み込みしても一覧（「金沢グルメ旅」1件）が保持されている。**ただしプランは自動で開かれず、トップは初期状態**（スプリント1の挙動を維持）。一覧の「開く」で初めて復元される。

## 受け入れ基準の結果

| 基準 | ステータス | 備考 |
|------|-----------|------|
| 保存済みプランの一覧が表示され、各プランの名前または行き先が確認できる | PASS | `SavedPlansPanel` に名前（`data-testid="saved-plan-name"`）＋行き先（📍）＋主要条件を表示。件数バッジ `saved-plans-count` あり。 |
| 異なる条件で作成した2つ以上のプランをそれぞれ保存でき、一覧に複数件として表示される | PASS | 「💾 このプランを保存」は毎回別エントリを `addPlan` で追加。2件以上が `saved-plan-item` として並ぶ。ロジック検証で件数増加を確認。 |
| 一覧からプランを選んで開くと、その内容が画面に表示される | PASS | 「開く」(`open-plan`)→ `handleOpenPlan` が保存スナップショットを `setPlan`。`useEffect([plan])` が `editableDays` を再構築し、保存時点の並び順・削除状態でタイムライン表示。条件フォームも同期。 |
| プランの名前を変更でき、変更後の名前が一覧に反映される | PASS | 「名前変更」(`rename-plan`)→ インラインinput→ Enter/保存(`rename-save`)で `renamePlan`。空文字は無視。localStorage に同期。 |
| プランを削除でき、削除後は一覧から消える | PASS | 「削除」(`delete-plan`)→ `deletePlan` で配列から除去、件数減。表示中プランを削除すると「表示中」状態も解除。 |

## 実施した検証

- `npm run build`: 成功（40 modules transformed、エラーなし）。
- `node server/index.js` 起動 → `GET /`=200、`GET /api/options` 正常、`POST /api/plan`（金沢/グルメ）で `structuredDays`・`mode:mock` を確認（実APIキー無しでもモックで動作）。
- ビルド済みバンドルに「保存済みプラン」「名前変更」「`saved-plans-panel`/`open-plan`/`rename-plan`/`delete-plan`」を含むことを確認（変更が反映済み）。
- `useSavedPlans` のデータ操作（移行・追加・開く・名前変更・空名無視・削除・リロード相当の再読込）を React 外で再現したロジックテストを実行 → 全項目 PASS。特にスプリント6の単一保存（`tabinavi.savedPlan.v1`）が配列へ移行され旧キーが削除されることを確認。

## 既知の問題・補足

- 名前入力に `window.prompt` を使用（保存時のみ）。テスト環境で `prompt` が抑止される場合は既定名で保存される実装にしてあり、保存自体は失敗しない。名前変更は一覧内のインライン入力（prompt 非依存）で行えるため、基準検証に支障はない。
- スプリント6の単一スロット保存・復元は「複数プラン管理」へ発展的に置き換えた。旧データは初回マウント時に一覧へ自動移行するため、既存ユーザーの保存プランは失われない。お気に入り（`useFavorites`）・編集（`editableDays`）・生成/再生成・エラー処理は変更しておらず、既存機能は維持。
- 「リロードで自動的にプランを開かない」というスプリント1の不変条件は維持（一覧の保持と画面の自動復元は別。一覧は残るが画面はトップ初期状態）。

## 信頼スコア
総合: 9/10
（自動ブラウザ操作は評価者の Playwright に委ねるため。サーバー・ビルド・永続化ロジックは検証済み。UIワイヤリングは静的確認＋データロジック検証で裏付け。）

## エバリュエーターへの引き渡し可否
YES — http://localhost:5180/ で上記「一連の受け入れ確認手順」を実施すれば全基準を確認可能。
