# スプリント6 自己評価

対象: スプリント6 — お気に入りとプランの保存・復元（機能10・機能11）
生成モード: サンプル生成モード（`ANTHROPIC_API_KEY` 未設定 / `mode: mock`）
起動: `npm start`（`vite build` → Express が `dist` と API を 5180 で配信）。`http://localhost:5180/` で操作可能。

## 実装の要点
- 永続化は全てブラウザ内 localStorage（DB・認証なし）。ヘルパー: `src/lib/storage.js`。
- お気に入り: `src/hooks/useFavorites.js`（安定キー = 行き先::スポット名）。各スポットに ☆/★ トグル（`src/components/DayTimeline.jsx`）。一覧パネル `src/components/FavoritesPanel.jsx`。
- 保存・復元: `editableDays`（編集後の並び順・削除を反映）を `App` に巻き上げ、保存スナップショットに取り込む（`src/App.jsx`）。`PlanResult` に「💾 このプランを保存」を追加（`src/components/PlanResult.jsx`）。
- 自動復元はしない。リロード後はトップが初期状態。保存があればトップに「保存済みプラン」バーが出て、明示操作で読み込む（スプリント1の初期状態維持と両立）。

## 受け入れ基準の結果
| 基準 | ステータス | 備考 |
|------|-----------|------|
| 各スポットにお気に入り登録の操作があり、登録するとお気に入り状態が視覚的に分かる（アイコンの変化等） | PASS | 各スポットに ☆→★ トグルボタン（`data-testid="fav-toggle"`）。登録時は ★ + オレンジ系の `spot__fav--on` スタイル＋ラベル「お気に入り済」に変化。`aria-pressed` も切替。 |
| お気に入りに登録したスポットの一覧を確認できる画面・領域がある | PASS | トップ直下に折りたたみ式「お気に入りスポット」パネル（`data-testid="favorites-panel"`）。件数バッジ・各項目（名前・行き先・タグ）・個別解除（✕）・全解除を備える。localStorage に同期するためリロード後も保持。 |
| 現在のプランを保存する操作があり、保存後にページを再読み込みしても保存したプランを読み込んで再表示できる | PASS | 結果上部「💾 このプランを保存」（`data-testid="save-plan"`）で localStorage に保存。リロード後トップに「保存済みプラン」バー＋「📂 保存したプランを読み込む」が出て再表示できる。トップ自体は初期状態のまま（スプリント1基準を維持）。 |
| 復元したプランが、保存時点の編集内容（並び順・削除状態）を反映している | PASS | 保存時に編集後の `editableDays` を uid 除去・日番号連番化して `structuredDays` として保存。復元は `plan` 差し替え → `App` の `useEffect([plan])` が保存済み構造から `editableDays` を再構築するため、保存時点の並び順・削除が反映される。 |

## 確認手順（エバリュエーター向け / Playwright を想定）

### A. お気に入り（基準1・2）
1. `http://localhost:5180/` を開く。トップに「お気に入りスポット 0件」パネルがあることを確認（空メッセージ表示）。
2. 行き先「京都」、泊数「2泊3日」、テーマ任意で「プランを作成」。結果のタイムラインが出る。
3. 任意のスポットの「☆ お気に入り」ボタン（`fav-toggle`）をクリック → アイコンが ★ に変わり、ボタンがオレンジ系（`spot__fav--on`）＋ラベル「お気に入り済」に変化することを確認（視覚変化）。
4. 「お気に入りスポット」パネルの件数が「1件」に増え、当該スポット名が一覧（`favorite-item`）に表示されることを確認。
5. もう一度同じボタンを押すと ☆ に戻り、パネルから消える（トグル解除）ことを確認。再度1件登録しておく。
6. **リロード**してもお気に入りパネルに登録済みスポットが残っている（localStorage 永続化）ことを確認。

### B. プラン保存・編集反映・復元（基準3・4）
1. プランを生成した状態で、**編集を加える**: あるスポットを「🗑 削除」、別のスポットを「↑/↓」で並べ替える。「全Nスポット」「各日 N件」が編集に追従して減少／変化することを確認（スプリント5の整合）。
2. 結果上部「💾 このプランを保存」（`save-plan`）をクリック → 通知（`data-testid="notice"`）「現在のプランを保存しました…」が表示される。
3. **ページを再読み込み**する。
   - トップが**初期状態**（行き先入力欄が空、結果非表示）に戻ることを確認（スプリント1基準を壊していない）。
   - トップに「保存済みプラン: 京都（保存日時）」バーが表示されることを確認。
4. 「📂 保存したプランを読み込む」をクリック → 保存したプランが再表示される。
   - 手順1で**削除したスポットが復元後も無い**こと、**並べ替えた順序が保存時点のまま**であること、「全Nスポット」「各日 N件」が保存時点と一致することを確認（＝保存時点の編集内容を反映）。
5. 任意: 「保存を削除」でバーが消えること、再リロードで保存済みバーが出ないことを確認。

### C. リグレッション確認（既存機能を壊していないこと）
- スポット詳細アコーディオン（スプリント4）: スポット見出しクリックで説明・おすすめポイント・滞在目安が開く。
- 削除・並べ替え・件数整合（スプリント5）: お気に入り追加後も編集操作・件数表示が正常。
- 再生成（スプリント4・5）: 「🔄 このプランを再生成」で編集・お気に入り表示のうち編集状態がリセット、お気に入り登録は維持（仕様上お気に入りは別管理）。
- リロードで初期状態（スプリント1）: 保存していなければリロードでトップが空に戻る。

## 既知の問題・設計上の判断（曖昧さの記録）
- お気に入りの一意キーは「行き先＋スポット名」。同一行き先・同名スポットは同一お気に入りとして扱う（重複登録されない）。仕様は同一性の粒度を明示していないため、最もシンプルで安定な解釈を採用した。
- 保存できるプランは1件（最新のもので上書き）。複数プランの一覧管理はスプリント7（機能12）のスコープのため、本スプリントでは単一スロット保存に留めた。
- お気に入りは「プランの保存・復元」とは独立して常時 localStorage に同期する（プラン未保存でも残る）。お気に入りはプランをまたぐ集合という用語集の定義に沿った判断。
- 復元したプランのタイムラインは再び編集可能（保存はあくまでスナップショット）。再度保存すると上書きされる。
- お気に入りトグルのラベル文言は ☆「お気に入り」／★「お気に入り済」。色（オレンジ）とアイコンでも状態が分かるようにしている。

## 信頼スコア
総合: 9/10
（理由: 4基準すべてをコードレベル＋API/ビルド/配信で確認済み。UI のクリック操作の最終検証はエバリュエーターの Playwright に委ねるため満点ではなく 9。）

## エバリュエーターへの引き渡し可否
YES — `npm start` 起動・`http://localhost:5180/` 到達・API 正常・ビルド成功を確認済み。上記手順で全4基準を検証可能。
