# スプリント5 自己評価

対象: スプリント5 — プランの編集（スポットの削除・並べ替え）（機能9）
実装日: 2026-06-01
モード: サンプル生成モード（`mode: mock` / `ANTHROPIC_API_KEY` 未設定）でも全基準を満たす
起動コマンド: `npm install && npm start`（`vite build` 成功・35 modules）
アプリURL: http://localhost:5180/

## 実装サマリ
- 編集はフロントエンドの状態管理で実装（サーバー・API は無変更。後方互換維持）。
  - `src/components/PlanResult.jsx`: `plan.structuredDays` を編集可能なローカル状態 `editableDays` として保持。各 spot に安定 `uid` を付与し、削除・並べ替え後も React の key と詳細展開状態がずれないようにした。`useEffect([plan])` で **plan が変わったとき（新規生成・再生成）のみ** リセット → 非生成操作（スクロール・日タブ切り替え）では編集を保持。
  - 操作ハンドラ: `handleDeleteSpot`（その日から除去）、`handleMoveUp` / `handleMoveDown`（日内入れ替え。日の端にあるスポットは隣の日へ移す＝日程間の入れ替え）。
  - 件数・見出しの整合: 各日見出しに「N件」（`day-count-{N}` testid）、日タブに「N日目（M件）」、条件行に「全Nスポット」（`total-spots` testid）を表示し、いずれも `editableDays` から都度再計算。日番号「N日目」は日インデックスから再計算（削除で日が空でも連番維持、空の日は専用メッセージ）。
  - `src/components/DayTimeline.jsx`: 各スポットに「↑」「↓」「🗑 削除」ボタンを追加（`aria-label` 付き）。先頭日の先頭の↑、最終日の末尾の↓は `disabled`。
  - `src/styles/global.css`: 編集ボタン・件数バッジ・空の日・編集案内のスタイルを追加（既存デザインに調和）。

## 受け入れ基準の結果
| 基準 | ステータス | 備考 |
|------|-----------|------|
| 各スポットに削除操作があり、削除するとそのスポットがタイムラインから消える | PASS | 各スポットに「🗑 削除」ボタン（`aria-label="〇〇を削除"`）。クリックで `editableDays` から該当スポットを除去し、再描画でタイムラインから消える。 |
| スポットの順序を変更する操作（上下移動またはドラッグ等）があり、操作後にタイムライン上の並び順が変わる | PASS | 各スポットに「↑」「↓」ボタン（`aria-label="〇〇を上へ移動"/"下へ移動"`）。日内は隣接スポットと入れ替え、日の端では隣の日へ移動（日程間入れ替え）。操作後に並び順が即時変化。 |
| 編集後の状態が、別の操作（スクロールやタブ切り替え等）を行っても保持されている | PASS | 編集状態は `editableDays`（ローカル状態）に保持。日タブ切り替え・スクロールでは `editableDays` を変更しないため保持。`useEffect` のリセットは `plan` 変化時（生成・再生成）のみ。 |
| 削除・並べ替えの操作後、ページ内の日程の見出しや件数表示が編集結果と整合している | PASS | 日見出し「N件」、日タブ「N日目（M件）」、「全Nスポット」を `editableDays` から都度算出。削除・移動・日程間移動・日が空になるケースでも整合。 |

## 確認手順（エバリュエーター向け）
1. `npm install && npm start` → http://localhost:5180/ を開く。
2. 行き先「京都」、泊数「2泊3日」、テーマ「歴史」で「プランを作成」。3日分・各4スポット・「全12スポット」と各日「4件」が表示される。
3. **削除**: いずれかのスポットの「🗑 削除」を押す → そのスポットが消え、その日の「N件」と日タブの件数、「全Nスポット」が1減る。
4. **並べ替え（日内）**: あるスポットの「↑」または「↓」を押す → 同じ日の中で隣のスポットと並び順が入れ替わる。
5. **並べ替え（日程間）**: 1日目の末尾スポットの「↓」を押す → 2日目の先頭へ移動し、1日目は1件減・2日目は1件増。逆に2日目先頭の「↑」で1日目末尾へ戻る。
6. **保持の確認**: 上記編集後に日タブを切り替える／ページをスクロールする → 並び順・削除結果がそのまま維持される（リセットされない）。
7. **整合の確認**: 何度か編集し、各日「N件」の合計が「全Nスポット」と一致すること、空になった日が「すべて削除されました」と表示されることを確認。
8. **リセットの確認（仕様どおり）**: 「🔄 このプランを再生成」を押すと編集はリセットされ、新規の構成に戻る（基準では非生成操作での保持のみ要求。再生成リセットは許容）。

## 既知の問題 / 仕様判断（曖昧さへの対応）
- 並べ替えは Playwright で安定して検証できるよう**上下移動ボタン**で実装（ドラッグ＆ドロップは未実装）。spec は「上下移動またはドラッグ等」を許容しており、補足でも上下移動ボタンを推奨。
- 「編集後の状態が別の操作でも保持」について、**再生成は編集をリセットしてよい**と spec 補足にあるため、`plan` 変化時のみリセットする方針を採った。スクロール・タブ切り替えなどの非生成操作では保持される。
- 日程間移動を許可したことで、編集により1日のスポットが0件になり得る。日番号「N日目」は連番を維持し、0件の日は専用メッセージを表示して件数（0件）と整合させる方針とした（日自体の削除は本スプリントの基準外のため行わない）。
- ブラウザへの永続保存（リロードで復元）はスプリント6（機能11）の範囲のため本スプリントでは未対応。リロードで初期状態に戻るのはスプリント1の基準どおり。
- 既存機能（行き先連動・泊数で日数分・テーマで内容変化・人数反映・日別タイムライン・タグ・詳細展開・再生成・エラー処理）は変更ロジックを加えていない／後方互換で維持。`vite build` 成功・API レスポンス（structuredDays）確認済み。

## 信頼スコア
総合: 9/10

## エバリュエーターへの引き渡し可否
YES — 4基準すべて実装済み。`npm start` で http://localhost:5180/ にて削除・上下移動（日内／日程間）・件数整合・編集保持を操作確認できる。
