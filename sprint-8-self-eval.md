# スプリント8 自己評価（エクスポートと予算の目安）

対象機能: 機能13（プランのエクスポート）・機能14（予算の目安表示）

## 実装サマリ
- 新規 `src/lib/export.js`: `buildPlanText(plan, days)` / `copyToClipboard(text)` / `downloadText(filename, text)` / `buildFileName(plan)`。
- 新規 `src/lib/budget.js`: `estimateBudget(plan, days)` / `formatYen(amount)`。
- 新規 `src/components/BudgetEstimate.jsx`: 予算の内訳・合計・1名あたりを表示。
- 変更 `src/components/PlanResult.jsx`: アクション群に「📋 コピー」「⬇️ ダウンロード」を追加。コピー／ダウンロード結果を `result__export-toast`（4秒で自動消滅）で通知。`BudgetEstimate` を結果画面内に常時表示。
- 変更 `src/styles/global.css`: エクスポートボタン・トースト・予算パネルのスタイル追加。
- サーバー・API・既存の保存／編集／お気に入り／複数プラン管理の挙動は変更なし（フロント完結）。

## 受け入れ基準の結果
| 基準 | ステータス | 備考 |
|------|-----------|------|
| プランをコピーまたはダウンロードできる操作があり、実行すると行き先・日程・スポットを含む内容が出力される（コピー成功通知 or ファイル取得が確認できる） | PASS | 「📋 コピー」(`copy-plan`) と「⬇️ ダウンロード」(`download-plan`) を実装。コピー成功時は `export-toast` に「✅ …コピーしました」を表示。ダウンロードは Blob→aタグ で `.txt` を取得。内容は `buildPlanText` が行き先・日程・各日スポットを出力。 |
| 出力された内容に画面上のプランの主要情報（行き先・各日のスポット名）が含まれている | PASS | 出力テキストに「行き先: 京都」「■ 1日目」と各スポットの時刻・名称・タグ・説明を含む。Node 実行で確認済み（下記）。`days` は編集後 `editableDays` 由来のため削除・並べ替え後の状態を反映。 |
| プランに対して概算費用の目安（移動・宿泊・食事・観光などの内訳または合計）が表示される | PASS | `BudgetEstimate`（`budget-estimate`）が移動・宿泊・食事・観光の4内訳＋合計（`budget-total`）＋1名あたり（`budget-per-person`）を表示。 |
| 条件（泊数や人数）を変えて生成すると、表示される費用の目安もそれに応じて変化する | PASS | 宿泊=単価×泊数×人数、食事=単価×日数×人数、移動=単価×人数、観光=単価×スポット数×人数。Node 実行で 1泊2日/2名=合計¥59,600、2泊3日/4名=合計¥180,800 と変化を確認。 |

## 確認手順（エバリュエーター向け）
起動: `npm install && npm start` → http://localhost:5180/

### エクスポート
1. 行き先「京都」を入力し「プランを作成」。結果画面が表示される。
2. 結果上部の「📋 コピー」を押す → `export-toast`（`data-testid="export-toast"`）に「✅ プラン内容をクリップボードにコピーしました…」が表示される（4秒で自動的に消える）。
   - Playwright では `page.evaluate(() => navigator.clipboard.readText())` でコピー内容を取得して「京都」「1日目」やスポット名が含まれることを検証できる（要クリップボード権限。権限が無い環境では下記ダウンロードで検証）。
3. 「⬇️ ダウンロード」を押す → `tabinavi_京都.txt` がダウンロードされ、`export-toast` に「⬇️ …ダウンロードしました。」が表示される。
   - Playwright では `page.waitForEvent('download')` でダウンロードを捕捉し、`download.suggestedFilename()` と保存ファイルの中身（行き先・各日のスポット名）を検証できる。
4. スポットを削除／並べ替えてから再度「ダウンロード」すると、編集後の状態がテキストに反映される。

出力テキストの例（`buildPlanText` を Node で実行して確認済み）:
```
========================================
たびナビ 旅行プラン
========================================
行き先: 京都
日程: 1泊2日
人数: 2名
テーマ: おまかせ（王道）

■ 1日目
  09:00 京都・…エリアの名所［定番・…］
    （説明文）
  …
```

### 予算の目安
1. 結果画面に「💰 予算の目安」パネル（`data-testid="budget-estimate"`）が表示される。移動・宿泊・食事・観光の各金額と「合計（目安）」「1名あたり」が出る。
2. 一度トップに戻り、人数を 2→4、泊数を 1→2 に変えて再生成すると、各内訳・合計の金額が増える（`budget-total` の値が変化）。
   - 検証用 testid: `budget-amount-transport` / `budget-amount-lodging` / `budget-amount-meals` / `budget-amount-sightseeing` / `budget-total` / `budget-per-person`。
3. 例: 京都・1泊2日・2名・標準テーマ → 合計 ¥59,600 / 1名 ¥29,800。京都・2泊3日・4名 → 合計 ¥180,800 / 1名 ¥45,200。

## 既知の問題・補足
- クリップボード API はブラウザの権限やセキュアコンテキストに依存する。`navigator.clipboard` が使えない場合は `execCommand('copy')` にフォールバックし、それも失敗すれば「⚠️ コピーに失敗しました。…ダウンロードをお試しください」と通知する。確実な観測手段としてダウンロード経路も併設しているため、エクスポート基準はダウンロードだけでも満たせる。
- 予算は「目安」であり固定単価ベースの概算（移動¥8,000/人、宿泊¥9,000/人泊、食事¥4,000/人日、観光¥600/人スポット）。テーマでは金額を変えていない（基準は泊数・人数での変化を要求しているため）。スポット数を編集で増減させても観光費が連動して変化する。
- 構造化データが無いフォールバック（API 生テキスト）でも、予算は `nights+1` を日数とみなして算出し破綻しない。エクスポートも `plan.planText` を本文に含めるためスポット情報は失われない。

## 信頼スコア
総合: 9/10

## エバリュエーターへの引き渡し可否
YES — 全4基準を満たす実装を確認済み。ビルド成功（`npm run build`）、`npm start` で 5180 起動・API 正常応答を確認。コピー成否はトーストで観測可能、ダウンロードは Blob→aタグ で取得可能、予算は条件で変化することを Node 実行で確認済み。
