# スプリント3 評価レポート

対象: スプリント3 — 日程タイムラインと結果の読みやすい表示（機能4: 日程別タイムライン / 機能17: おすすめタグ表示）
評価日: 2026-06-01
評価者: エバリュエーター（Playwright による実動作検証）
実行モード: サンプル生成モード（`mode: mock` / `ANTHROPIC_API_KEY` 未設定）
起動コマンド: `npm install && npm start`
アプリURL: http://localhost:5180/

## 判定: PASS

## 検証環境
- `npm install && npm start` で正常起動。ビルド成功、Express が `dist/` と API を 5180 で配信。
- Playwright（Chromium ヘッドレス, viewport 1280x1600）で実ブラウザ操作。
- 自己評価は鵜呑みにせず、フォーム入力・タブ操作・スクロール・DOM タグ数のカウント・API 直接呼び出しまで自分で確認した。

## 受け入れ基準の結果
| # | 基準 | 結果 | 証拠 |
|---|------|------|------|
| 1 | 生成結果が日別（1日目・2日目…）のセクションに分かれて表示される | PASS | 京都・2泊3日で `.dayblock` が 3 セクション（1日目/2日目/3日目）。各見出しに連番バッジ。`/tmp/sprint3-shots/02-result-full.png` |
| 2 | 各日のセクション内に、時間帯または順序とスポット名・説明が並んだタイムライン状の表示がある | PASS | 各日 `<ol class="timeline">` に 4 スポット。時刻（09:00/12:00/14:00/18:00、最終日は16:30）＋スポット名＋説明＋ドット・縦線のタイムライン UI を確認。`02-result-full.png` |
| 3 | 各スポットに特徴を示すタグ（例:「絶景」「グルメ」など）が1つ以上表示される | PASS | 全 12 スポットを DOM 上で集計し、`.spottag` が各 4 個（合計 全スポット tagCount>=1、タグなしスポット 0 件）。例: 自然「絶景/朝がおすすめ/みんなで/写真映え」。API でも各日4スポット×タグ4を確認。 |
| 4 | 複数日のプランで、日のセクションを切り替えまたはスクロールして全日程を閲覧できる | PASS | `.daytab`（1日目/2日目/3日目）を表示。3日目タブ押下で最終日セクションがビューポート内へスクロール＆`daytab--active`が3日目に移動、1日目タブで戻ることを確認。スクロールでも全3セクション到達可。`/tmp/sprint3-shots/03-tab-lastday.png` |

**通過率: 4/4 (100%)**

## 発見したバグ
### P0
- なし

### P1
- なし

### P2
- なし（軽微な外観上の問題も再現せず）

## リグレッション
検出なし。スプリント1・2の主要インタラクションを再確認:
- 行き先連動: 札幌で生成すると全スポット名に「札幌・」が反映（destination reflected = true）。
- 泊数→日数: 2泊3日=3セクション / 1泊2日=2セクション / 日帰り(nights=0)=1セクション（タブ非表示・tabs=0）と整合。
- テーマ変更で内容変化: 自然→温泉で再生成し、スポット名・タグ・説明がすべて変化（namesDiffer=true, tagsDiffer=true。温泉では「温泉/朝風呂・温泉街/名湯の宿」等に変化）。
- 人数反映: people=3 で「みんなで」タグを確認。
- ローディング表示: 送信直後にローディングインジケータを検出（loading indicator seen = true）。
- 再読み込みで初期化: リロード後に入力欄が空（destVal=""）、結果非表示（hasResult=false）。

## 技術的検証ログ（観測事実）
- コンソールエラー: 0 件（トップ・結果・タブ操作・再生成・リロードの全工程）
- ページ JS エラー: 0 件
- ネットワーク失敗 / 4xx・5xx: 0 件
- `POST /api/plan`（金沢/2泊3日/4名/歴史）の直接確認: `mode: mock`, `days:3`, `structuredDays` 3 日分・各日 4 スポット・各スポット `tags` 4 個。全スポットがタグ 1 個以上を保持。

## 推奨改善事項（ブロッキングではない・任意）
1. ヘッドレス Chromium 環境ではフォント未導入により一部の日本語グリフが豆腐（□）で描画されることがある。DOM テキスト自体は正しく、アプリの不具合ではない（評価では `$$eval` で実テキストを取得して確認済み）。実機ブラウザでは問題なし。本番表示の堅牢性のため、CSS `font-family` に汎用日本語フォントのフォールバックを明示しておくとより安全。
2. 日タブは「該当日へスクロール＋アクティブ強調」の補助で、各日セクションは常時描画される（フィルタ型ではない）。受け入れ基準「切り替えまたはスクロール」は満たすが、将来的に長い行程では特定日のみ表示するフィルタ型も選択肢。
3. タイムラインの時刻列は固定スロット（09:00/12:00/14:00/18:00）。将来 API 実モードでの可変時刻にも UI が耐える点は確認済みだが、移動時間の表現などを足すとさらに読みやすくなる。

## スプリント1・2基準の維持（明示確認）
- 行き先入力欄・「プランを作成」ボタン: あり。
- 泊数 select（日帰り〜5泊6日）/ 人数 number ＋増減ボタン / テーマ select（おまかせ・グルメ・自然・歴史・温泉・子連れ）: すべて表示・操作可能。
- いずれもスプリント3の表示刷新後も破壊されていない。
