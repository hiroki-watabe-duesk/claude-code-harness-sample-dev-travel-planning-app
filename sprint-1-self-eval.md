# スプリント1 自己評価

## 概要
スプリント1のゴール「行き先を入力してボタンを押すと、AIが生成した旅行プランが画面に表示される最小の中核体験」を実装した。
React + Vite フロントエンドと Express バックエンドで構成し、プラン生成は `server/planGenerator.js` の単一インターフェース `generatePlan()` に抽象化した。
`ANTHROPIC_API_KEY` 設定時は Claude API を呼ぶ主経路、未設定時は行き先に応じて内容が変わるサーバー側モックにフォールバックする。

## 確認手順（再現方法）
1. プロジェクトルートで `npm install`（初回のみ）。
2. `npm start` を実行（`vite build` 後に Express が起動）。
3. ブラウザで **http://localhost:5180/** を開く。
4. 入力欄に「京都」と入力し「プランを作成」を押す → ローディング表示後にプランが出る。
5. 「札幌」でも試す → 京都と異なる内容になることを確認。
6. ページを再読み込み → 入力欄が空・結果非表示の初期状態に戻る。

API 単体でも確認可能:
- `curl -X POST http://localhost:5180/api/plan -H "content-type: application/json" -d '{"destination":"京都"}'`
- `curl -X POST http://localhost:5180/api/plan -H "content-type: application/json" -d '{"destination":"札幌"}'`

## 受け入れ基準の結果
| 基準 | ステータス | 備考 |
|------|-----------|------|
| トップ画面に行き先テキスト入力欄と「プランを作成」ボタンが表示されている | PASS | `#destination` 入力欄と `.planform__button`（「プランを作成」）を確認。Playwright で初期表示を検証済み。 |
| 行き先を入力してボタンを押すとローディング表示が現れる | PASS | クリック直後に `.loading`（スピナー＋「『京都』の旅行プランを作成中…」）が表示されることを Playwright で確認。 |
| 生成完了後、入力に応じた旅行プランが表示される（固定文でなく入力連動） | PASS | 「京都」と「札幌」で本文・見どころ・コース文面が明確に異なる。行き先名が本文に織り込まれる。固定文ではない。 |
| ページ再読み込みで初期状態（入力欄が空のトップ画面）に戻る | PASS | リロード後、入力欄が空・結果0件であることを Playwright で確認。永続化を行っていないため初期化される。 |

## API キー方針への対応
- 主経路（実API）: `generateWithClaude()` で Anthropic Messages API を呼び出す。既定モデルは `claude-opus-4-20250514`（`ANTHROPIC_MODEL` で変更可）。後から `.env` に実キーを差すだけで本番動作する設計。
- フォールバック（モック）: `generateMockPlan()` が行き先名のハッシュから雰囲気・午前/昼/午後/夜のスポット文を決定的に選び、入力ごとに異なる動的プランを生成。「京都」と「札幌」で異なる結果を確認済み。
- UI に「サンプル生成モード」バッジを控えめに表示（モック時のみ）。
- キーはハードコードせず `.env`（`.gitignore` 済み）から読み込み。`.env.example` を同梱。

## 既知の問題 / 留意点
- 実API（live）モードは API キーを持たないため、本環境では実呼び出しの動作確認は未実施（コードパスは実装済み・分離済み）。モックモードで全基準を満たすことを確認済み。
- スプリント1の範囲に限定し、日数・人数・テーマ（スプリント2）、タイムラインUI（スプリント3）などは未実装（意図的なスコープ外）。
- プランは1日分のモデルコース文面。複数日対応はスプリント2で追加予定。

## 信頼スコア
総合: 9/10

## エバリュエーターへの引き渡し可否
YES — `npm start` で http://localhost:5180/ から操作可能。全受け入れ基準を Playwright による実ブラウザ操作で確認済み（ページエラーなし）。
